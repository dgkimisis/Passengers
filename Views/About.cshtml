@model List<Passengers.Models.Passenger>

<!DOCTYPE html>
<html>
<head>
    <title></title>
    <meta charset="utf-8" />
</head>
<body>
    @for (int i = 0; i < Model.Count; i++)
    {
        Url.Action("GetAbout", Model[i].FirstName);
        Url.Action("GetAbout", Model[i].LastName);
    }

</body>
</html>


